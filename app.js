// const { dispatch } = require('nact');
// const greeter = require('./stateless-actor');
// const statefulGreeter = require('./statefull-actor');
// const { ping, pong } = require('./src/actors/actor-communication');
require('./src/api');

// dispatch(greeter, { name: "MHF" });
// dispatch(statefulGreeter, { name: "MHF2" })
// dispatch(ping, 'begin', pong);
// console.log(GET_CONTACT);
